import React from 'react';
import Navigation from "./Navigation";

function Header() {
    return (
        <header>
            <a href='#'>Логотип</a>
            <Navigation />
        </header>
    );
}

export default Header;